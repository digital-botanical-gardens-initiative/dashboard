<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DBGI - <%=id%></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="https://unpkg.com/@rdkit/rdkit/dist/RDKit_minimal.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <script type="text/javascript" language="javascript" src="/JSME_2022-09-26/jsme/jsme.nocache.js"></script>
    <style>
        #molecule {
          display: inline-block;
          background-color: white;
          border: 1px solid #ccc;
          padding: 10px;
          margin: 10px;
        }
        .navbar {background-color: green;}
        tr {border-bottom: 2px solid #ddd;}
        table {width: 100%;}
        .organisms-grid {
            display: grid;
            grid-template-columns: repeat(3, auto);
            grid-gap: 10px;
        }
        .organism-item {
            list-style-type: none;
            width: 25em; /* the element needs a fixed width (in px, em, %, etc) */
            overflow: hidden; /* make sure it hides the content that overflows */
            white-space: nowrap; /* don't break the line */
            text-overflow: ellipsis; /* give the beautiful '...' effect */
        }
        ul {
            columns: 3;
            -webkit-columns: 3;
            -moz-columns: 3;
        }
    </style>
</head>
<body>
  <%- include('./partials/nav.ejs') %>

  <div class="container">
    <h2><%= id %> (<a href="<%= wikidata %>"><%= wikidata_id %></a>)</h2>
    <br>
    <h4>Chemical structures</h4>
    <h6>molecular formula: <%= formula %></h6>
    <div class="row">
        <div class="col-md-6">
          <div code="JME.class" archive="JME.jar" width=240 height=180>
            <param name="options" value="depict,useopenchemlib">
            <param name="smiles" value="<%= smile%>">
          </div>
          <figcaption style="text-align: center;">3D</figcaption>
        </div>
        <div class="col-md-6">
          <div code="JME.class" archive="JME.jar" width=240 height=180>
            <param name="options" value="depict,useopenchemlib">
            <param name="smiles" value="<%= smile_2d%>">
          </div>
          <figcaption style="text-align: center;">2D</figcaption>
        </div>
    </div>
    </div>
    <hr>
    <h4>Phylogeny</h4>
    <table>
      <tr>
          <th>Kingdom</th>
          <td><%=kingdom%></td>
      </tr>
      <tr>
          <th>SuperClass</th>
          <td><%=superclass%></td>
      </tr>
      <tr>
          <th>Class</th>
          <td><%=structure_class%></td>
      </tr>
      <tr>
          <th>Direct parent</th>
          <td><%=directparent%></td>
      </tr>
    </table> 
    <hr>
    <h4>Present in:</h4>
    <div class="organisms-grid">
      <ul>
        <% organisms.forEach((organism, index) => { %>
          <li class="organism-item">
            <a href="/organism/<%= encodeURIComponent(organism)%>">
              <%- organism %>
            </a>
          </li>
        <% }); %>
      </ul>
    </div>
  </div>
</body>
</html>
